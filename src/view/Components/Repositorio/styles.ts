import styled from "styled-components";

export const RepositorioContainer = styled.a`
  padding: 10px;
  border-radius: 8px;
  text-decoration: none;

  color: var(--text-title);

  background-color: rgba(0, 0, 0, 0.041);

  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
`;
